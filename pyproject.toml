[project]
name = "portfolio-site"
version = "0.1.0"
description = "Build tooling for the personal portfolio site: converts content/*.md into styled HTML posts and refreshes the Writing index listings."
requires-python = ">=3.13"
dependencies = [
    "markdown>=3.10",
]

[tool.uv]
package = false
